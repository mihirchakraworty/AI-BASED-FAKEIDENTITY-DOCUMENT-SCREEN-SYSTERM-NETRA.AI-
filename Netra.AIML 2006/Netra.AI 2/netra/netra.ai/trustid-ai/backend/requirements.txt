fastapi
uvicorn[standard]
python-multipart

pillow
numpy
opencv-python-headless
pytesseract

# Optional but strongly recommended for real face matching.
# Requires dlib (needs cmake + a C++ compiler to build).
#face_recognition